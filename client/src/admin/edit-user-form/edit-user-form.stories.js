import * as React from 'react';
import { EditUserForm } from './edit-user-form.component';

export default { title: 'Admin/EditUserForm' };

const Template = args => <EditUserForm {...args} />;

export const Default = Template.bind({});
