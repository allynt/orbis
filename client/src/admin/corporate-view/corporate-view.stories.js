import * as React from 'react';
import CorporateView from './corporate-view.component';

export default { title: 'Admin/Corporate View' };

const Template = args => <CorporateView {...args} />;

export const Default = Template.bind({});
