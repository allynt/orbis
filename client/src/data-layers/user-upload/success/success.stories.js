import React from 'react';

import { Success } from './success.component';

export default { title: 'Data Layers/User Upload/Success' };

const Template = args => <Success {...args} />;

export const Default = Template.bind({});
Default.args = {};
