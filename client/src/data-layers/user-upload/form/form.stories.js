import React from 'react';

import { Form } from './form.component';

export default { title: 'Data Layers/User Upload/Form' };

const Template = args => <Form {...args} />;

export const Default = Template.bind({});
Default.args = {};
