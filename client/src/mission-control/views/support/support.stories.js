import React from 'react';

import { Support } from './support.component';

export default { title: 'Mission Control/Support' };

const Template = args => <Support {...args} />;

export const Default = Template.bind({});
Default.args = {};
